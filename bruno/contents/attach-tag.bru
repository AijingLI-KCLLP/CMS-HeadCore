meta {
  name: attach tag to content
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/contents/{{contentId}}/tags
  body: json
  auth: inherit
}

body:json {
  {
    "tag_id": {{tagId}}
  }
}

settings {
  followRedirects: false
}
