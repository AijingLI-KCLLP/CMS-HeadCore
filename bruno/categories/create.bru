meta {
  name: create category
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/categories
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Mode"
  }
}

settings {
  encodeUrl: false
  timeout: 0
  followRedirects: false
}
